<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Books to Read</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>

<div class="container">
    <h1>Books to Read</h1>
    <ul>
        <% bookList.forEach((book, index) => { %>
            <li>
                <%= book.title %> by <%= book.author %> (<%= book.genre %>)
                <% if (book.read) { %>
                    <span style="color: rgb(163, 67, 201);">(Read)</span>
                <% } else { %>
                    <a href="/books/<%= index %>/read">Mark as Read</a>
                <% } %>
                <a href="/books/<%= index %>/edit"><button>Edytuj</button></a>
                <a href="/books/<%= index %>/remove">Remove</a>
            </li>
        <% }); %>
    </ul>
    <a href="/books/new">Add New Book</a>
</div>

</body>
</html>